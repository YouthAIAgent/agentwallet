# AgentWallet Protocol - Environment Configuration

# Database
DATABASE_URL=postgresql+asyncpg://agentwallet:agentwallet@localhost:5432/agentwallet

# Redis
REDIS_URL=redis://localhost:6379/0

# Solana
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet

# Platform wallet (receives fees)
PLATFORM_WALLET_ADDRESS=

# Encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# JWT
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24

# API
API_HOST=0.0.0.0
API_PORT=8000
API_CORS_ORIGINS=http://localhost:5173

# Stripe (optional for billing)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Rate limits
RATE_LIMIT_DEFAULT=60

# AWS KMS (optional, for production key encryption)
AWS_KMS_KEY_ID=
AWS_REGION=us-east-1

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json
